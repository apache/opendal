// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

// DO NOT EDIT IT MANUALLY. This file is generated by opendal/dev/src/generate/dotnet.rs.

use std::ffi::c_char;

/// FFI representation of `opendal::Metadata`.
///
/// Fields follow the accessor order of `opendal::Metadata`. Optional values
/// flatten into a `*_has_value` byte, `1` when present, followed by the parts
/// that are meaningful only then. String pairs are parallel arrays of `*_len`
/// owned C strings, or null when there are no pairs.
#[repr(C)]
pub struct OpendalMetadata {
    /// Return this entry's mode.
    pub mode: i32,
    /// Return whether this metadata describes the current object version.
    pub is_current_has_value: u8,
    pub is_current: u8,
    /// Return whether this metadata describes a deleted object or delete marker.
    pub is_deleted: u8,
    /// Return this entry's Cache-Control value.
    pub cache_control: *mut c_char,
    /// Return the full content length of this entry.
    pub content_length: u64,
    /// Return this entry's Content-MD5 value.
    pub content_md5: *mut c_char,
    /// Return this entry's Content-Type value.
    pub content_type: *mut c_char,
    /// Return this entry's Content-Encoding value.
    pub content_encoding: *mut c_char,
    /// Return this entry's Last-Modified timestamp.
    pub last_modified_has_value: u8,
    pub last_modified_second: i64,
    pub last_modified_nanosecond: i32,
    /// Return this entry's service-provided ETag as-is.
    pub etag: *mut c_char,
    /// Return this entry's Content-Disposition value.
    pub content_disposition: *mut c_char,
    /// Return this entry's service-specific version identifier.
    pub version: *mut c_char,
    /// Return a borrowed view of this entry's user metadata.
    pub user_metadata_has_value: u8,
    pub user_metadata_keys: *mut *mut c_char,
    pub user_metadata_values: *mut *mut c_char,
    pub user_metadata_len: usize,
}

impl OpendalMetadata {
    pub fn from_metadata(metadata: opendal::Metadata) -> Self {
        let mode = crate::utils::entry_mode_code(metadata.mode());
        let (is_current_has_value, is_current) = crate::utils::optional_bool(metadata.is_current());
        let is_deleted = u8::from(metadata.is_deleted());
        let cache_control = crate::utils::optional_c_string(metadata.cache_control());
        let content_length = metadata.content_length();
        let content_md5 = crate::utils::optional_c_string(metadata.content_md5());
        let content_type = crate::utils::optional_c_string(metadata.content_type());
        let content_encoding = crate::utils::optional_c_string(metadata.content_encoding());
        let (last_modified_has_value, last_modified_second, last_modified_nanosecond) =
            crate::utils::optional_timestamp(metadata.last_modified());
        let etag = crate::utils::optional_c_string(metadata.etag());
        let content_disposition = crate::utils::optional_c_string(metadata.content_disposition());
        let version = crate::utils::optional_c_string(metadata.version());
        let (user_metadata_has_value, user_metadata_keys, user_metadata_values, user_metadata_len) =
            crate::utils::string_pairs(metadata.user_metadata());

        Self {
            mode,
            is_current_has_value,
            is_current,
            is_deleted,
            cache_control,
            content_length,
            content_md5,
            content_type,
            content_encoding,
            last_modified_has_value,
            last_modified_second,
            last_modified_nanosecond,
            etag,
            content_disposition,
            version,
            user_metadata_has_value,
            user_metadata_keys,
            user_metadata_values,
            user_metadata_len,
        }
    }
}

/// Release the heap-allocated fields of a metadata value in place.
///
/// Entries store metadata inline, so their release path frees the fields
/// without also freeing a containing box.
/// # Safety
///
/// - `metadata` must have been produced by `from_metadata`.
/// - Must be called at most once for the same value.
pub(crate) unsafe fn metadata_release_fields(metadata: &mut OpendalMetadata) {
    unsafe {
        crate::utils::release_c_string(&mut metadata.cache_control);
        crate::utils::release_c_string(&mut metadata.content_md5);
        crate::utils::release_c_string(&mut metadata.content_type);
        crate::utils::release_c_string(&mut metadata.content_encoding);
        crate::utils::release_c_string(&mut metadata.etag);
        crate::utils::release_c_string(&mut metadata.content_disposition);
        crate::utils::release_c_string(&mut metadata.version);
        crate::utils::release_string_pairs(
            &mut metadata.user_metadata_keys,
            &mut metadata.user_metadata_values,
            &mut metadata.user_metadata_len,
        );
    }
}
