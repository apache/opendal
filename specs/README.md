# OpenDAL Services Specs

`specs` is the single truth of the opendal service's specification, which is used to generate the service's code and documentation.

## Validate

```shell
taplo check specs/*.toml
```
